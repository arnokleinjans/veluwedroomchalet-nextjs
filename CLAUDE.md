<!-- deploy-regel-v1 -->
> **⛔ Deploy-regel (geldt voor ALLE projecten):** Push NOOIT naar GitHub en deploy NOOIT naar Vercel of enige andere hosting/remote, tenzij Arno daar in dít bericht expliciet om vraagt. Lokaal committen en branchen mag; maar `git push`, het aanmaken van remote repos, `vercel`/deploy-commando's, en het wijzigen van remote secrets/omgevingsvariabelen vereisen telkens een nieuwe, expliciete opdracht. Toestemming voor één push/deploy geldt niet voor de volgende. Bij twijfel: eerst vragen.

# Veluwedroomchalet

> **Token-check vóór elke taak:** Beoordeel eerst of de huidige chat al veel context bevat. Als een nieuwe chat token-efficiënter is — meld dat dan **meteen** aan de gebruiker, zonder code te lezen of iets uit te voeren.
>
> Nieuwe chat is slim bij: losse/nieuwe taken, lange conversatiegeschiedenis, wisseling van onderwerp.
> Doorwerken in dezelfde chat is slim bij: directe follow-up op wat net gedaan is.

Next.js web app voor chaletbeheer en gastcommunicatie.

## Stack
- Next.js (App Router), TypeScript, Tailwind CSS
- Dynamische routing: `/b/[bookingId]/` voor per-boeking pagina's
- Chat, info, omgeving, video pagina's per boeking

@AGENTS.md

## Asset Generation (Nano Banana Design)

Voor het genereren van nieuwe 3D bladeren voor de video-instructies, gebruik de volgende prompt template (bijv. in Midjourney, DALL-E of Gemini):

> Ultra-realistic highly detailed 3D render of a single **[NAME]** leaf, **[SPECIFIC SHAPE/CHARACTERISTICS]**, vibrant **[COLORS]**. Macro photography vividly showing thick intricate veins. Flat direct lighting. The leaf is perfectly isolated on a solid #ffffff purely white background with sharp edges.

**Gebruikte bladeren:**
- **Oak:** eikenblad, lobed edges, autumn green/brown
- **Maple:** esdoornblad, pointed lobes, orange/red
- **Monstera:** split-leaf design, deep jungle green
- **Birch:** berkenblad, heart-shaped, summer green
- **Chestnut:** kastanjeblad, spiky/long, fresh green
- **Beech:** beukenblad, rich deep purple/burgundy

---

## Instructies voor Claude

1. **Geen comments schrijven** tenzij de reden niet-voor-de-hand-liggend is.
2. **Niet meteen bouwen** bij vage vragen — eerst de aanpak voorleggen.
3. **Volledige rechten.** De gebruiker heeft Claude toestemming gegeven om alle shell-commando's uit te voeren zonder bevestiging te vragen.
4. **Changelog bijhouden.** Elke aanpassing aan de codebase moet worden geregistreerd in de sectie `## Changelog` onderaan dit bestand. Formaat: datum + korte omschrijving.

---

## Changelog

| Datum | Omschrijving |
|---|---|
| 2026-07-31 (stappenplan) | Check-in/check-out-infographics van de camping vervangen door een vertaalbaar stappenplan. Probleem: de poster (595×841) schaalde op een telefoon terug naar 310px breed, waardoor de ingebakken tekst ~6px hoog werd, én de Nederlandse poster stond óók in de EN/DE-versie. Oplossing: de 12 tekeningen uit `bronposters/check in NL.png` en `check out NL.png` geknipt (Python/PIL, segmentatie op kleur — tekst is puur zwart, tekeningen zijn groen/bruin; witte randen weggeknipt, achtergrond transparant gemaakt via flood-fill vanaf de rand zodat wit bínnen de tekening blijft), geëxporteerd op 2× als `public/images/Check{in,out} stap 1-6.webp` + `... uitleg.webp`. Nieuw `stappen`-veld op een insight (`{image, tekst, zonderNummer}`) + `app/components/Stappenplan.tsx` (grid, 1 kolom mobiel / 2 kolommen ≥720px, genummerde badge, `zonderNummer` = toelichting over volle breedte) + stijl in `globals.css`. Beheer: uitklapbaar blok "🪜 Stappenplan" per tegel met tekeningkiezer, tekstveld, volgorde-knoppen en verwijderen. Teksten NL/EN/DE rechtstreeks in Redis gezet (incl. `translations.en/de`) en de oude poster-`<img>` uit `detailContent` gestript; "slagboompas" bewust onvertaald gelaten in EN/DE (staat zo op het fysieke pasje). E2E geverifieerd tegen productiebuild: 6 kaarten + 1 toelichting per plan, geen gebroken afbeeldingen, mobiel/desktop en NL/EN/DE gescreenshot. Testboekingen TEST-STAP-EN en TEST-STAP-DE (30-07 t/m 05-08) op verzoek van Arno laten staan. |
| 2026-07-31 (vervolg) | Privacylek in de gastpagina's gedicht: `getTranslatedAppData()` geeft de hele `veluwe_app_data`-blob terug en die ging ongefilterd door naar client-componenten, waardoor de paginabron van élke gastlink alle 50 boekingen bevatte (naam, aankomst/vertrek, boekingscode) inclusief 4 volledige nachtregistraties (adres, postcode, woonplaats, telefoon, e-mail), plus `accessCode` (VELUWE2026) en `chatbotContext`/`aiPrompt`. Nieuwe `toClientAppData()` in `app/utils/db.ts` strijkt `bookings`/`accessCode`/`chatbotContext`/`aiPrompt`/`aiMaxChars`/`translations` eruit; toegepast op de 3 plekken die appData aan een client-component doorgeven (2× `app/b/[bookingId]/layout.tsx`, 1× `info/page.tsx`). Vooraf geverifieerd dat géén client-component die velden leest (het beheerpaneel haalt zijn data los op via `fetchAdminData`) en dat de vertaalmerge vóór het strippen gebeurt. Bijvangst: payload van ~158 KB naar 66 KB. Getest tegen een echte productiebuild (dev-modus serialiseert extra debug-data en geeft een vals-positief): 7 gastroutes tonen geen enkel gestript veld en geen enkele andere gastnaam meer, terwijl de pre-arrival-gate (52677, 53954/en), de verlopen-pagina (MARINA-UVZ9) en de 404 blijven werken; info/omgeving visueel 1-op-1 vergeleken met live. Nog NIET gedeployed. |
| 2026-07-31 | Pre-arrival-gate in `app/b/[bookingId]/layout.tsx` opgeheven zodra de nachtregistratie de status `verstuurd` heeft: de gast ziet dan meteen de volledige app i.p.v. alleen het formulier, ook vóór (aankomst − 2 dagen) 08:00. Aanleiding: boeking 53021 (Jolanda Rietbergen, aankomst 03-08) was al verstuurd maar zat nog achter de gate. Status `ingevuld` houdt de gate bewust wél dicht (registratie kan nog worden aangepast). Geverifieerd op de echte Redis-data: `/b/53021` geeft 200 met de normale navigatie, de rode nachtregistratie-tegel blijft verborgen. Gepubliceerd op expliciete opdracht Arno. |
| 2026-07-24 (avond, fix) | Bug gefixt die Arno zag na het verwijderen van een afbeelding: een tegel toonde kort de onmogelijke status "In gebruik (0)" (verdween na een pagina-refresh). Oorzaak: `loadMediaLibrary()` riep `listMediaImages()` en `getImageUsageMap()` parallel aan, en `getImageUsageMap()` deed daarbinnen zijn eigen tweede `listMediaImages()`-call — vlak na een verwijder-commit kon GitHub die twee net verschillend beantwoorden, waardoor een tegel wel in de lijst zat maar geen usage-entry had; de render viel dan terug op de "In gebruik"-tak met teller 0. Fix: `getImageUsageMap(imageNames?)` werkt nu op de meegegeven momentopname (aanroeper haalt de lijst één keer op en geeft de namen door) — consistent én een dubbele GitHub-API-call minder. Daarnaast de render weerbaar gemaakt: onbekende status toont nu "⏳ Status laden..." i.p.v. zich voor te doen als "In gebruik (0)". Geverifieerd tegen live data: 120 afbeeldingen, 0 zonder usage-entry, 0 met "used maar 0 locaties". |
| 2026-07-24 (avond) | UI-verbeteringen mediabibliotheek + alle systeem-popups vervangen (3 wensen Arno). (1) Nieuw `app/components/ImageLightbox.tsx`: klik op een thumbnail in de mediabibliotheek toont de afbeelding schermvullend (klik/Esc om te sluiten, `cursor: zoom-in` op de thumbnail). (2) Bezig-indicatie: draaiende zandloper-overlay over de tegel tijdens verwijderen (`vdcSpin`-keyframes) + zandloper in de uploadknop; na een geslaagde verwijdering wordt naast de bibliotheek nu ook de dropdown-lijst en de preview-map ververst (deed hij alleen na upload). (3) Nieuw `app/components/ConfirmDialog.tsx` — herbruikbare dialoog in de huisstijl (Lora-titel, groen/rood accent, blur-overlay, Esc/Enter-afhandeling, optionele `details`-lijst, en zonder `onConfirm` fungeert hij als nette alert-vervanger). Daarmee **alle 7 native `confirm()`/`alert()`-aanroepen in de app vervangen**: mediabibliotheek verwijderen + foutmelding, "Boeking definitief verwijderen?" (toont nu ook de gastnaam), 2× AI-samenvatten-foutmelding, en de 2 verstuur-bevestigingen in `NachtregistratieAdminPanel.tsx`. Ook de al bestaande inline modals (upload-naamconflict, "nog in gebruik"-blokkade) getrokken naar dezelfde stijl/z-index. Geverifieerd: `npx tsc --noEmit` schoon en `grep` op `confirm(`/`alert(` levert niets meer op in `app/`. |
| 2026-07-24 (nog later, vervolg) | Twee bugs gefixt n.a.v. Arno's eerste echte upload-test (Acim.webp) in de mediabibliotheek. (1) PIN ontbrak bij upload/delete-server-actions (`invalid-pin`): `pin`-state werd na een pagina-herlaad leeggemaakt (localStorage bewaart alleen het login-vlaggetje, niet de PIN) — PIN-parameter volledig verwijderd uit `uploadMediaImage`/`deleteMediaImage`, nu consistent met alle andere admin-server-actions in dit bestand die ook geen server-side PIN-herverificatie hebben. (2) Het GitHub-token bleek een fine-grained PAT zonder Contents-schrijfrechten op déze repo (`403 Resource not accessible by personal access token` op zowel PUT als DELETE, terwijl GET altijd werkte omdat de repo publiek is) — mijn eerdere "token werkt ook hier"-check (het `permissions`-veld op de repo-endpoint) bleek onvoldoende, dat weerspiegelt de rol van de gebruiker, niet de scope van het specifieke token. Arno heeft een nieuw fine-grained token aangemaakt, specifiek gescoped op `veluwedroomchalet-nextjs` met Contents: Read and write (30 dagen geldig, tot 2026-08-23) — bevestigd werkend met een live PUT+DELETE-test (aangemaakt en meteen weer opgeruimd). (3) UI-polish: "In gebruik"-disclosure toonde geen driehoekje meer (had per ongeluk `display:"block"` i.p.v. het standaard `list-item`, wat de native `<summary>`-marker verbergt) — teruggezet naar `list-item` + `listStylePosition:"inside"` zodat het pijltje links van de tekst staat, met behoud van de knop-achtige breedte/achtergrond-styling. (4) Preview-thumbnails in de bestaande plaatjekiezers (header/home-item/video/omgeving) toonden een kapot plaatje voor een net-geüploade, nog niet lokaal-gesyncte afbeelding (die kiezers bouwden altijd een lokaal pad, ongeacht of het bestand al lokaal bestond) — nieuwe `getImagePreviewMap()` in `mediaActions.ts` (lookup `images/Naam.webp` → de al bestaande `previewUrl`-logica) gekoppeld aan alle 4 preview-`<img>`-tags. Ontdekt onderweg: bij lokaal testen met `npm run dev` blijft een verse upload altijd op "⏳ wordt gepubliceerd" staan totdat je zelf `git pull` doet — een Vercel-deploy update alleen de productie-site, niet Arno's lokale checkout; geen bug, wel vermeldenswaardig voor toekomstig lokaal testen. |
| 2026-07-24 (nog later) | Mediabibliotheek fase 2+3 (uploaden + verwijderen) gebouwd bovenop fase 1, per `docs/PRD-mediabibliotheek.md`. `.env.local` uitgebreid met `GITHUB_TOKEN` (hergebruikt uit Zaakmodule — bevestigd via GitHub API dat dit token ook push/admin-toegang heeft tot `veluwedroomchalet-nextjs`) + `GITHUB_REPO`. `app/actions/mediaActions.ts` uitgebreid: `ghFetch()`-wrapper (zelfde header-patroon als het al werkende `Zaakmodule/app/api/github/*`), `uploadMediaImage()`/`deleteMediaImage()` die via de GitHub Contents API committen naar `public/images` op main (Vercel's filesystem is read-only, dus dit kan niet rechtstreeks), `isGithubConfigured()` voor de UI-fallback zonder token, en `listMediaImages()` merget nu de GitHub-directorylijst met de lokale fs-lijst voor een "⏳ wordt gepubliceerd"-indicator (`isLive`) + `previewUrl` (toont de net-gecommitte versie via raw.githubusercontent.com zolang de deploy nog niet klaar is — repo is publiek, dus dat werkt zonder extra auth). Nieuw `app/utils/imageNormalize.ts`: client-side normalisatie (canvas/createImageBitmap, max 1920px, webp kwaliteit 0.85, geen server-side sharp i.v.m. HEIC-ondersteuning) — bewuste afwijking van de PRD's `sanitizeFileName`-voorstel (kebab-case/lowercase): behoudt i.p.v. daarvan dezelfde lichte regel als vandaag in `beheer.py` doorgevoerd (alleen eerste letter hoofdletter, rest van de naam intact), zodat er geen twee verschillende naamgevingsconventies naast elkaar ontstaan. UI in `page.tsx`: upload-knop (multi-file, sequentieel verwerkt met statusregels), conflict-modal (Overschrijven/Hernoemen/Annuleren) bij een bestaande naam, verwijder-knop per tegel (geblokkeerd + locatie-lijst als de afbeelding nog in gebruik is, met server-side defense-in-depth-check vlak vóór het echte verwijderen). `assetActions.ts`: `fetchAvailableHeaderImages()` gaat nu via `listMediaImages()` zodat een verse upload direct in de bestaande dropdowns verschijnt, met fs-fallback. `next.config.ts`: `bodySizeLimit: "4mb"` voor server actions (base64-payloads van genormaliseerde webp's). Bug onderweg gevonden en gefixt vóór testen: eerste versie gebruikte `process.env.NEXT_PUBLIC_GITHUB_REPO` in de client-component (bestond niet) voor de "pending"-preview-URL — verplaatst naar de server (`previewUrl`-veld op elk MediaImage, server-side samengesteld met het echte `GITHUB_REPO`). Getest: `npx tsc --noEmit` schoon, en de read-kant (`listMediaImages`/`getImageUsageMap`) rechtstreeks tegen de live GitHub-repo + Redis-data gedraaid via een los tsx-script — 129 afbeeldingen via de GitHub+fs-merge, gebruik-telling exact gelijk aan fase 1 (32 gebruikt/97 ongebruikt), alle `isLive: true` (lokaal = GitHub, geen pending items). De schrijf-kant (upload/delete) is NIET door Claude zelf tegen de live repo getest — dat zou een echte git-commit + Vercel-deploy triggeren, wat onder de deploy-regel valt (nooit zonder expliciete opdracht per keer); code-patroon is 1-op-1 overgenomen van het al bewezen werkende Zaakmodule-GitHub-endpoint. NIET gedeployed: `GITHUB_TOKEN`/`GITHUB_REPO` staan alleen lokaal in `.env.local`, nog niet in Vercel-project-settings. |
| 2026-07-24 (later) | Mediabibliotheek fase 1 gebouwd in het beheerpaneel (`docs/PRD-mediabibliotheek.md`, plan lag al klaar sinds 12 juni): nieuwe sectie "🖼️ Mediabibliotheek" (lazy-loaded, ná "⏰ Verlopen Boeking Pagina") toont alle 129 afbeeldingen in `public/images` met per stuk een "gebruikt/niet gebruikt"-check. Nieuw `app/actions/mediaActions.ts`: `listMediaImages()` (fs.readdirSync + bestandsgrootte) en `getImageUsageMap()` — doorzoekt in één `getAppDataFresh()`-call zowel de exacte image-velden (headerImage/thumb/omgeving.image/insights.image) als de rich-text-HTML (omgeving.desc/insights.detailContent/expiredPageContent), en dat apart voor NL + `translations.en`/`.de` zodat een taal die uit sync raakt zichtbaar wordt i.p.v. gemaskeerd (precies wat gisteren met qr.webp misging). Plus een vaste lijst voor de ~4 hardcoded codebase-referenties (Header.webp/logo/db.ts-fallbacks) die geen Redis-content zijn. Getest met een los tsx-script rechtstreeks tegen de live Redis-data (niet alleen tsc): 97 ongebruikt/32 gebruikt, en de vier gisteren hernoemde bestanden (Qr.webp/Qr-2/Qr-3/Deproeftuinhoenderloo) tonen correct als gebruikt in alle 3 talen. Nog NIET gebouwd (bewust, volgende stap): uploaden en verwijderen — die vereisen de GitHub Contents API-opzet uit de PRD (Vercel's filesystem is read-only, dus dat kan niet rechtstreeks). Zaakmodule heeft dat patroon al werkend (`app/api/github/upload-asset`, `delete`) en het GitHub-token van Zaakmodule blijkt ook volledige push-toegang tot veluwedroomchalet-nextjs te hebben — herbruikbaar zodra fase 2/3 wordt opgepakt. NIET gedeployed (lokaal getest, wacht op akkoord Arno). |
| 2026-07-24 (vervolg) | Live Redis-content gefixt na de bestandshernoeming van dezelfde dag: 4 velden in `veluwe_app_data` verwezen nog naar de oude kleine-letter-bestandsnamen (`qr.webp`/`qr-2.webp`/`qr-3.webp` in `insights[8].detailContent` NL + de EN/DE-vertaling van dezelfde stap, en `omgeving[1].image` = `images/deproeftuinhoenderloo.webp`). Dit brak zichtbaar in productie (kapotte plaatjes op `/b/[bookingId]/info/home/8`) omdat Vercel (Linux) hoofdlettergevoelig is, i.t.t. lokaal macOS. Root cause: deze content leeft in Redis (Upstash, key `veluwe_app_data`), niet in de codebase — de eerdere codebase-brede grep-zoekactie kon dit dus niet vinden. Gefixt met een eenmalig Node-script (rechtstreeks via `@upstash/redis`, met vooraf een lokale JSON-backup van de volledige dataset in `scripts/redis_backup_*.json`), daarna leeg-scripts opgeruimd. Geen deploy nodig — dit is live app-data, geen code. Les: bij toekomstige bestandshernoemingen ALTIJD ook de Redis-content (`veluwe_app_data`, incl. `translations.en`/`translations.de`) controleren op image-referenties, niet alleen de codebase. |
| 2026-07-24 | Snelplaatje in `antigravity-beheer/beheer.py` (menu 8) forceert voortaan een hoofdletter als eerste teken bij het normaliseren van afbeeldingsnamen — aanleiding: een mix van hoofdletter/kleine letter in `public/images/` maakte zoeken in het beheerscherm lastig. 16 bestaande bestanden hernoemd (`git mv`) en de ene code-referentie (`app/globals.css`, login-achtergrond `header.webp`→`Header.webp`) bijgewerkt; `npx tsc --noEmit` schoon. Gepubliceerd op expliciete opdracht Arno (commit `4921189a`, gepusht). |
| 2026-07-21 (vervolg, deploy) | Nachtregistratie-mail naar de camping krijgt voortaan een BCC naar info@veluwedroomchalet.nl (`mailer.ts`) — aanleiding: Arno zag verzonden mails niet terug in Outlook (SMTP-only versturen via nodemailer schrijft geen kopie naar Verzonden Items, dat doet alleen een los mailprogramma zelf via IMAP); BCC zorgt voor een tastbare kopie in de Inbox als bewijs van verzending. Gepubliceerd op expliciete opdracht Arno. |
| 2026-07-21 (deploy) | Beheer-boekingenoverzicht: CSV-import-dubbelcheck aangescherpt van alleen naam naar naam+aankomstdatum (aanleiding: Sandra Buijs met een nieuwe periode werd onterecht als dubbel gezien) — handmatig toevoegen controleerde al nooit op naam. Filterbalk kreeg een derde tab "Alle" (naast Nieuw/Verlopen) en een naam-zoekveld, zodat een boeking met een foutieve/verlopen datum altijd vindbaar blijft. Gepubliceerd op expliciete opdracht Arno: commit gepusht → Vercel auto-deploy. |
| 2026-07-19 (nacht, deploy) | Nachtregistratie naar productie op expliciete opdracht Arno. Alle 5 lokale commits (f7e14269..abe37f90) gepusht → Vercel auto-deploy. SMTP_HOST/PORT/USER/PASS (Hostnet) toegevoegd aan Vercel Production env vars (stonden nog alleen lokaal) + extra `vercel deploy --prod` zodat de build de nieuwe env vars ook echt meekrijgt. Live geverifieerd: homepage/beheer/bestaande gastlink/nachtregistratieroute 200, onbekende code 404. Bewust NIET aangepast: camping-e-mailadres staat nog op het testadres akleinjans@me.com (Arno vult het echte receptie-adres zelf in via beheer → Algemene Informatie zodra hij dat heeft) — tot die tijd komt een "Verstuur naar camping" bij hemzelf terecht, geen risico op een verkeerd verstuurde registratie. |
| 2026-07-19 (nacht, later) | Twee kleine nachtregistratie-fixes n.a.v. Arno's doorloop. (1) Datums in de gastenlinks-lijst en de CSV-importpreview stonden in ISO-notatie (2026-07-19) i.p.v. Nederlands — nu via `formatDatumNL` (DD-MM-YYYY) op beide plekken in de beheerpagina. (2) Mail naar de camping: "Periode: X t/m Y" gesplitst in aparte regels "Aankomstdatum:"/"Vertrekdatum:"; ondertekening "Arno Kleinjans" → "Kitty van der Pijll" (mailer.ts). Build + screenshot geverifieerd. |
| 2026-07-19 (nacht, vervolg) | "← Terug"-link toegevoegd boven het nachtregistratieformulier, consistent met de andere detailpagina's (zelfde stijl als de imageloze variant in info/home/[index]) — maar alleen in `app/b/[bookingId]/nachtregistratie/page.tsx` (de normale detailroute, bereikt via de rode tegel binnen de app). De pre-arrival-gate rendert `NachtregistratieForm` rechtstreeks vanuit layout.tsx buiten die route om, dus tijdens de 2 weken/D-2-periode vóór aankomst blijft het formulier terecht zonder terugknop. Geverifieerd met een tijdelijke testboeking binnen het toegankelijke venster (terugknop + navigatie zichtbaar) en met Karin's boeking (nog in pre-arrival, geen terugknop) — opgeruimd. |
| 2026-07-19 (nacht) | Nachtregistratie-nabranders n.a.v. doorloop Arno. (1) Alias-fallback voor oude slugs weer verwijderd (`app/utils/findBooking.ts` terug naar simpele id-match, `aliases[]` uit adminActions/beheer-UI) — overbodig gebleken, oude boekingen houden gewoon hun bestaande slug. (2) PDF-opmaakbug écht gefixt: AcroForm-tekstvelden bleven ondanks expliciete fontSize/DA-manipulatie auto-sizen (bedrag "47,50" bleef groot, ook bij fontSize 9 — pdf-lib's text-field appearance-regeneratie bleek het niet te respecteren, empirisch vastgesteld met hoge-resolutie pdftoppm-renders i.p.v. de te grove eerdere sips-previews). Nieuwe aanpak in `nachtregistratiePdf.ts`: form.flatten() op het blanco sjabloon, daarna alle waarden met page.drawText() op vaste x/y-posities (uit de bekende veldrechthoeken) — volledig deterministisch, geen AcroForm-gedoe meer. (3) Instellingen-duplicatie opgelost (Arno's melding: Naam verhuurder/Telefoon in de aparte "📋 Nachtregistratie"-sectie overlapten met Naam Huisje/WhatsApp Nummer in "Algemene Informatie"): aparte sectie weg, `verstuurNachtregistratie` leest voortaan `property.name`/`property.host.phone` rechtstreeks; alleen Staanplaats + camping-e-mail (nieuw op `property.staanplaats`/`property.campingEmail`) toegevoegd onderaan Algemene Informatie. Bijvangst: tijdens het herstructureren schoof een Fast-Refresh hook-order-bug een foutieve waarde ("Veluwe Droom Chalet") in staanplaats bij een save vanuit Arno's eigen open tabblad — rechtstreeks in Redis gecorrigeerd naar "Bosrand 18 / Klavergraslaan". (4) Teksten aangepast: "naar de camping" → "naar het recreatiepark", betaal-overmaken-optie ingekort naar "Ik maak het bedrag direct over aan de verhuurder" (nl-only, en/de ongewijzigd). (5) Nieuw icoon `public/icons/clipboard.svg` (zelfde lijnstijl) — wordt automatisch voorgesteld zodra de beheerder een insight-tegel op zichtbaarheid "Nachtregistratieformulier" zet (geen losse URL nodig, de tegel linkt zelf al naar `/b/[id]/nachtregistratie`). (6) Adresvelden gesplitst: gastformulier + beheerpaneel hebben nu ook verplichte Postcode/Woonplaats naast Adres; op de PDF worden ze samengevoegd tot één regel ("straat, postcode plaats"). Alles opnieuw E2E getest (build, PDF hoge-resolutie visueel gecontroleerd, API-validatie 400/200 met tijdelijke testboekingen, opgeruimd). NIET gedeployed. |
| 2026-07-19 (avond) | NACHTREGISTRATIE volledig gebouwd volgens docs/PRD-nachtregistratie.md. Gast: vóór (aankomst−2 dagen) 08:00 toont de gastlink uitsluitend het formulier (gate in layout.tsx, zelfde patroon als ExpiredBookingPage); daarna bereikbaar op /b/[id]/nachtregistratie. Formulier (NachtregistratieForm.tsx, i18n nl/en/de in translations.ts): betaalkeuze receptie/overmaken bovenaan, gast vult alleen naam (voorgevuld)/adres/telefoon/e-mail/personen, datums+kostenberekening (€10 + €6,25/volgnacht, app/utils/toeristenbelasting.ts) automatisch; Opslaan → grijs + Aanpassen; na verstuurd gelockt. Opslag via POST /api/nachtregistratie (rate-limited, zonder PIN, gekeyed op boekingscode). Beheer: statusbadge leeg/ingevuld/verstuurd per gastenlink + filter Nieuw/Verlopen (default Nieuw) + uitklappaneel (NachtregistratieAdminPanel.tsx: alles bewerkbaar, opmerkingen-default per betaalkeuze, Verstuur naar camping) + instellingen-sectie 📋 Nachtregistratie (verhuurdergegevens/camping-e-mail). Verzenden vult het originele PDF-formulier (app/assets/, pdf-lib; veldmapping Text1-26 via scripts/inspect-pdf-fields.mjs; widget-DA's verwijderd + vaste 11pt anders werd opmerkingen enorm; geflattened, alleen p.1) en mailt via nodemailer/Hostnet-SMTP (env SMTP_*). Insight-zichtbaarheidsoptie "📋 Nachtregistratieformulier": rode tegel, alleen zichtbaar zolang status leeg, linkt naar het formulier. Alias-functie van eerder vandaag weer verwijderd (besluit Arno: oude boekingen houden oude slug, geen overgang nodig). E2E lokaal getest: PDF visueel gecontroleerd (PNG), testmail met bijlage naar akleinjans@me.com verstuurd, gate/opslaan/validatie/404 via tijdelijke TEST-NREG-boeking (opgeruimd). Camping-e-mail staat nog op testadres akleinjans@me.com. NIET gedeployed. Naschrift: formulierkaart kreeg display:block-override omdat de globale .card-klasse een flex-rij afdwingt (layout lag eerst door elkaar) + minWidth:0 op flex-kolommen; mobiel (390px) en desktop visueel geverifieerd via puppeteer-screenshots. |
| 2026-07-19 | Voyando-boekingsnummer als slug: beheer heeft nu een veld "Boekingsnummer" bij Nieuwe Link Aanmaken (optioneel — leeg = oude NAAM-XXXX-generatie) én in het Bewerk-paneel per boeking. Bij wijzigen schuift de oude code automatisch naar `aliases[]` zodat al gedeelde links blijven werken (getoond als "Oude link (werkt nog)" in beheer). Nieuwe centrale lookup app/utils/findBooking.ts (id óf alias, case-insensitief) vervangt alle 7 losse `bookings.find`-plekken (layout, db.getTranslatedAppData, info/omgeving/video-subpagina's — die matchten voorheen case-sensitief). Uniekheidscheck op nummer over ids+aliases in addBooking/updateBooking. Afspraak Arno: bestaande boekingen houden hun oude slug; Voyando-nummers pas vanaf boeking "Karin Ram-De Man". SMTP-gegevens Hostnet (info@veluwedroomchalet.nl) in .env.local gezet voor de komende nachtregistratie-mail. Gepubliceerd op expliciete opdracht: commit f7e14269 → GitHub (veluwedroomchalet-nextjs) → Vercel auto-deploy, live geverifieerd op webapp.veluwedroomchalet.nl. |
| 2026-07-19 | PRD toegevoegd: docs/PRD-nachtregistratie.md — gast vult nachtregistratieformulier 't Veluws Hof in via de webapp (vóór D−2 08:00 alleen het formulier zichtbaar), beheerder controleert/verzendt als ingevulde originele PDF (alleen p.1) per Hostnet-SMTP naar de camping; statusbadges leeg/ingevuld/verstuurd + insight-zichtbaarheidsoptie "nachtregistratie". Status: nog niet gebouwd, wacht op akkoord. |
| 2026-07-07 | Drietalige check-out infographic (NL/EN/DE) toegevoegd: public/images/"check out NL-EN-DE.webp" (1588×2246) + zelfde als print-PDF "check out NL-EN-DE.pdf". Nagebouwd in de stijl van "check out NL.webp" (origineel was te lage resolutie om te bewerken); HTML-bron in Claude-artifact, tekst per stap in 🇳🇱/🇬🇧/🇩🇪 met vlaggen. |
| 2026-06-12 | Nieuw icoon public/icons/swimmer.svg (zwemmer/zwembad) in dezelfde lijnstijl als de overige iconen. |
| 2026-06-12 | PRD toegevoegd: docs/PRD-mediabibliotheek.md — plan voor mediabibliotheek in het beheerpaneel (status: geparkeerd, nog niet gebouwd). |
